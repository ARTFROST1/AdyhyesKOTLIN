---

# 📱 Карта приложения **AdygGIS**

**Последнее обновление:** 2025-09-30  
**Версия:** v1.0.0 - Stage 11 Pre-Launch Preparation (96% Complete) 🎉  
**Статус:** 🎉 ПЕРВАЯ RELEASE СБОРКА УСПЕШНА! APK собран и протестирован. Готов к Google Play.

> Брендинг: Пользовательское имя приложения — «AdygGIS». Внутренние пакеты/классы продолжают использовать префикс «Adygyes», чтобы не ломать код.
> Иконка: адаптивная — фон `ic_launcher_background.xml` (зелёный градиент), передний план `ic_launcher_foreground.xml` (золотой компас, VectorDrawable на `<path>`).

## 🟦 Загрузочный экран — **SplashScreen**

**Содержимое:**
* **Логотип AdygGIS** с анимацией появления
* **Название приложения** и слоган
* **Индикатор загрузки** (CircularProgressIndicator)
* **🎬 ФОНОВАЯ ПОДГОТОВКА** - предзагрузка данных и изображений для мгновенной анимации

**Технические особенности:**
* **MapPreloadManager** - параллельная загрузка всех изображений достопримечательностей
* **Создание невидимых маркеров** с предзагруженными изображениями
* **ImageCacheManager** - кэширование bitmap в памяти для мгновенного доступа
* **Проверка версии данных** через PreferencesManager
* **Автоматический переход** после загрузки данных

**Действия:**
* 🔘 **Фоновая предзагрузка** → подготовка маркеров с изображениями
* 🔘 **Автоматический переход** → мгновенная анимация маркеров при переходе

## 🟩 Главный экран — **MapScreen с двухслойной системой маркеров**

**Архитектура:** Dual-Layer Marker System - нативные визуальные маркеры + Compose слой для кликов

**✨ ОБНОВЛЕНО: Навигация перенесена наверх**

**Содержимое:**

* **Карта (Yandex Maps API) / Список достопримечательностей** (плавное переключение с анимацией)
* **Верхняя панель навигации** (Material Design 3 Surface):
  - Кнопка переключения вида (карта/список) 📋 слева с анимированными иконками
  - **🎬 УЛУЧШЕННОЕ ПОЛЕ ПОИСКА** с кинематографическими анимациями по центру 🔍
    - Плавное расширение с spring-анимацией при фокусе
    - Режим-зависимые кнопки: сортировка/вид (список) или фильтры (карта)
    - AnimatedContent + SizeTransform для органичного растягивания
    - Cubic Bezier easing для профессиональной плавности
  - Кнопка настроек ⚙️ справа
* **✨ НОВАЯ КАРУСЕЛЬ КАТЕГОРИЙ** - горизонтальная прокрутка под поиском
* **✨ РЕЖИМЫ ОТОБРАЖЕНИЯ** - переключение список/плитки с компактными карточками как на экране избранного
* **Революционные маркеры** - двухслойная система с идеальной привязкой и 100% кликами
* **Геообъекты** (многоугольники парков, линии троп, водные объекты)
* **Кнопка "Моё местоположение"** с GPS позиционированием 📍
* **Edge-to-edge дизайн** с правильными WindowInsets

**Технические особенности:**
* **🎬 НОВАЯ СИСТЕМА АНИМАЦИИ МАРКЕРОВ** - премиальная плавность появления с предзагруженными изображениями
* **Dual-Layer Architecture** - нативные визуальные маркеры + Compose клики (100% надежность)
* **Dynamic Clustering** - автоматическая группировка маркеров при масштабировании
* **Circular Image Markers** - круглые маркеры с фотографиями достопримечательностей
* **MapPreloadManager** - предзагрузка данных и изображений во время splash screen
* **MapHost** - контейнер для MapView с управлением жизненным циклом
* **MapStateViewModel** - сохранение состояния камеры между сессиями
* **VisualMarkerRegistry** - реестр визуальных маркеров для переиспользования
* **DualLayerMarkerSystem** - оркестратор двухслойной системы маркеров
* **VisualMarkerProvider** - создание круглых маркеров с изображениями + ультра-плавная анимация
* **MarkerOverlay** - Compose слой для обработки кликов
* **ClusteringAlgorithm** - алгоритм кластеризации маркеров
* **🖼️ ImageCacheManager** - продвинутая система кэширования изображений (25% памяти, 250MB диск)
* **Hardware Bitmap Fix** - исправлена совместимость Canvas с `.allowHardware(false)`
* **Production Performance** - минимальные накладные расходы, оптимизированная производительность

**Действия:**

* 🔘 **Ввод в поле поиска** → мгновенная фильтрация на карте/в списке (debounced)
* 🔘 **✨ НОВОЕ: Карусель категорий** → горизонтальная прокрутка, выбор категории
* 🔘 **✨ НОВОЕ: Переключение режимов** → список/плитки в поле поиска (иконка GridView/ViewList)
* 🔘 **✨ НОВОЕ: Сортировка** → по имени/категории/рейтингу в выпадающем меню
* 🔘 **Верхняя кнопка: Переключение вида** → плавная анимация карта ↔ список
* 🔘 **Верхняя кнопка: Настройки** → переход в **SettingsScreen**
* 🔘 **Клик по метке/элементу списка** → **AttractionBottomSheet** (100% гарантированно)
* 🔘 **Масштабирование/перемещение карты** → обновление меток (оптимизировано)
* 🔘 **Кнопка «Моё местоположение»** → центрирование карты на пользователе с анимацией

---

## 🟦 Компонент — **AttractionBottomSheet** (Modal Bottom Sheet)

**Реализация:** AttractionBottomSheet.kt с Material Design 3

**Содержимое:**

* **PhotoGallery** - Галерея фото с swipe, zoom и lazy loading (кэшированные изображения)
* **Название достопримечательности** - Typography.headlineSmall
* **Краткое описание** - Scrollable content с полной информацией
* **Рейтинг** - RatingBar component с звездами ⭐
* **Контактная информация** - Часы работы, цены, телефон
* **Кнопки действий** (Material 3 buttons):
  * 🚩 **«Построить маршрут»** → NavigationUseCase → Яндекс.Карты
  * ⭐ **«Добавить в избранное»** → ToggleFavorite в ViewModel
  * 🔗 **«Поделиться»** → ShareUseCase → нативный share sheet

**Технические особенности:**
* **ModalBottomSheet** с rememberModalBottomSheetState
* **key(attraction.id)** для принудительного пересоздания
* **Drag handle** для удобного закрытия
* **Proper dismiss handling** с clearSelection в ViewModel

**Действия:**

* **Swipe down / tap outside** → onDismiss() → возврат на карту
* **Кнопка «Избранное»** → toggleFavorite() → обновление состояния
* **Кнопка «Поделиться»** → shareAttraction() → системное меню
* **Кнопка «Маршрут»** → navigateToAttraction() → внешняя навигация

---

## 🟨 Экран — **FavoritesScreen**

**📢 ВАЖНО:** Экран остаётся доступным, но прямой доступ временно убран из навигации.  
**Будущее:** Кнопка доступа будет добавлена в настройки.

**Содержимое:**
* **Переключатель режимов** - список/плитки (ViewMode)
* **Сортировка** - по имени, категории, рейтингу, дате добавления
* **Список избранных** - карточки с фото, названием, рейтингом
* **Статистика** - количество по категориям
* **Pull-to-refresh** - обновление списка
* **Swipe-to-delete** - удаление свайпом
* **Empty state** - сообщение при пустом списке

**Действия:**
* **Клик по карточке** → AttractionDetailScreen
* **Свайп влево** → удаление из избранного с анимацией
* **Pull-to-refresh** → обновление данных
* **Кнопка "Исследовать"** → возврат на карту

---

## 🟧 Компонент — **CategoryFilterBottomSheet** (Фильтрация категорий)

**Реализация:** CategoryFilterBottomSheet.kt с Material Design 3

**Содержимое:**

* **Header** с заголовком "Фильтры" и кнопкой "Очистить все"
* **LazyColumn** со списком всех категорий AttractionCategory.values()
* **Checkbox** для каждой категории с состоянием выбора
* **Apply button** для применения фильтров
* **ModalBottomSheet** с rememberModalBottomSheetState

**Технические особенности:**
* **Real-time filtering** - изменения применяются мгновенно
* **State management** через selectedCategories StateFlow
* **Category toggle** через ViewModel.toggleCategory()
* **Persistent state** - фильтры сохраняются при навигации

**Действия:**

* **Выбор категории** → toggleCategory() → мгновенная фильтрация на карте/списке
* **Кнопка "Применить"** → onApply() → закрытие bottom sheet
* **Swipe down / tap outside** → onDismiss() → закрытие без изменений

---

## 🟥 Экран — **SettingsScreen**

**Содержимое:**

### Настройки приложения:
* **Язык** - Русский (полностью локализован) / English (подготовлено)
* **Тема** - Светлая / Тёмная / Системная
* **Уведомления** - Новые места, обновления, события

### Настройки карты:
* **Моё местоположение** - включить/выключить GPS
* **Кластеризация маркеров** - группировка при масштабировании
* **Показывать трафик** - слой пробок (планируется)
* **Автоцентрирование** - следование за пользователем

### Данные и хранилище:
* **Очистить кэш изображений** - освобождение места
* **Размер кэша** - отображение занятого места
* **Автообновление данных** - при запуске приложения

### О приложении:
* **Версия** - текущая версия приложения
* **Политика конфиденциальности** - ссылка на документ
* **Условия использования** - правовая информация
* **Связаться с нами** - email поддержки

---

# 🔄 User Flow (Unified MapScreen Architecture)
{{ ... }}
```
[MapScreen.kt - Unified Main Screen]
   │
   ├── 🆙 TopNavigationBar (Material 3 Surface)
   │    ├── View Toggle 📋 (слева) → AnimatedContent(MAP ↔ LIST)
   │    ├── 🔍 UnifiedSearchTextField (по центру)
   │    │    ├── Real-time input → debounced search (300ms)
   │    │    ├── Clear button → onValueChange("")
   │    │    └── Filter button → CategoryFilterBottomSheet
   │    │         ├── Category checkboxes → toggleCategory()
   │    │         ├── "Clear All" → clearFilters()
   │    │         └── "Apply" → close sheet
   │    └── Settings ⚙️ (справа) → SettingsScreen
   │
   ├── 🗺️ Map Mode (Persistent MapHost + overlay)
   │    ├── MapHost: единый MapView с жизненным циклом и стилями (ночной режим)
   │    ├── DualLayerMarkerSystem: VisualMarkerProvider (нативные маркеры) + TransparentClickOverlay (клики)
   │    ├── VisualMarkerRegistry: инкрементальные обновления маркеров без полного пересоздания
   │    ├── Camera state persisted: MapStateViewModel + DataStore
   │    └── FAB 📍 → GPS location → camera animation
   │
   ├── 📋 List Mode (AttractionsList)
   │    ├── Filtered attractions → real-time updates
   │    ├── Item tap → onAttractionClick()
   │    ├── Favorite toggle → toggleFavorite()
   │    └── WindowInsets padding → proper layout
   │
   └── 🎯 selectedAttraction?.let → AttractionBottomSheet
        ├── key(attraction.id) → force recomposition
        ├── PhotoGallery → swipe & zoom
        ├── Attraction details → scrollable content
        ├── Action buttons:
        │    ├── 🚩 Route → NavigationUseCase → Yandex Maps
        │    ├── ⭐ Favorite → toggleFavorite() → state update
        └── onDismiss → clearSelection() → close sheet

Technical Features:
├── 🔧 reliable marker taps (100% guaranteed)
├── 🎨 Edge-to-edge UI with WindowInsets
   178→├── 🔄 Proper MapKit lifecycle (handled by MapHost)
   180→├── ⚡ Optimized marker updates (VisualMarkerRegistry incremental sync)
   182→├── Persistent MapHost (single MapView instance)
   183→├── Camera state persistence (MapStateViewModel + DataStore)
   184→├── Marker persistence (VisualMarkerRegistry + DataStore)
└── 🎭 Smooth animations (view mode transitions)

## 🟪 Экран — **SearchScreen**

**Содержимое:**
* **Поле поиска** с автофокусом и очисткой
* **Предложения поиска** - real-time подсказки
* **Недавние поиски** - история запросов
* **Популярные места** - топ достопримечательностей
* **Результаты поиска** - список найденных мест
* **Фильтры категорий** - чипсы для фильтрации

**Действия:**
* **Ввод текста** → debounced поиск (300ms)
* **Выбор подсказки** → мгновенный поиск
* **Клик по результату** → AttractionDetailScreen
* **Выбор категории** → фильтрация результатов
## 🟦 Экран — **AttractionDetailScreen**

**Содержимое:**
* **PhotoGallery** - галерея с swipe и zoom
* **Основная информация** - название, категория, рейтинг
* **Описание** - полный текст с прокруткой
* **Контакты** - адрес, телефон, сайт
* **Часы работы** - расписание по дням
* **Цены** - стоимость входа
* **Удобства** - список amenities с иконками
* **Теги** - flow layout с тегами

**Действия:**
* **Свайп по фото** → переключение изображений
* **Pinch-to-zoom** → увеличение фото
* **Кнопка "Избранное"** → добавление/удаление
* **Кнопка "Маршрут"** → открытие в Яндекс.Картах
* **Кнопка "Поделиться"** → системный share sheet

---

# 📌 Ключевые принципы UX

* **Главный экран — это карта с возможностью переключения на список.**
* **Поиск интегрирован в главный экран** для быстрого доступа.
* Минимум кликов до информации (метка/элемент → карточка).
* Все действия (избранное, маршруты, поделиться) доступны прямо из карточки.
* Поддержка работы оффлайн (закэшированные данные + базовые карты).
* ✅ **Полная локализация на русский язык** - весь интерфейс адаптирован для русскоязычных пользователей.

---

# 🎯 Статус реализации

## ✅ **Полностью реализовано:**

### 🗺️ **Карта и навигация:**
- ✅ Yandex MapKit v4.8.0 интеграция
- ✅ Двухслойная система маркеров (100% надёжность кликов)
- ✅ Динамическая кластеризация маркеров
- ✅ Bottom navigation с анимациями
- ✅ GPS позиционирование с разрешениями
- ✅ Сохранение состояния камеры

### 📱 **Экраны:**
- ✅ MapScreen - главный экран с картой/списком
- ✅ SearchScreen - поиск с подсказками
- ✅ FavoritesScreen - управление избранным
- ✅ SettingsScreen - все настройки
- ✅ AttractionDetailScreen - детали места
- ✅ SplashScreen - загрузочный экран

### 🎛️ **Компоненты:**
- ✅ AttractionBottomSheet - детали на карте
- ✅ CategoryFilterBottomSheet - фильтры
- ✅ CategoryCarousel - карусель категорий
- ✅ AttractionsList - список/плитки
- ✅ PhotoGallery - галерея с zoom
- ✅ SearchBar - поиск с подсказками
- ✅ RatingBar - отображение рейтинга
- ✅ EmptyState - пустые состояния
- ✅ LoadingShimmer - скелетоны загрузки
### 💾 **Данные и состояние:**
- ✅ 10+ реальных достопримечательностей Адыгеи
- ✅ Room database с миграциями
- ✅ DataStore для настроек
- ✅ Автоматическое версионирование данных
- ✅ Offline-first архитектура
- ✅ Repository pattern

### 🖼️ **Кэширование изображений:**
- ✅ Coil ImageLoader с оптимизацией
- ✅ Memory cache - 25% доступной памяти
- ✅ Disk cache - до 250MB
- ✅ Версионная инвалидация кэша
- ✅ Hardware bitmap fix для Canvas
- ✅ Lazy loading в галереях

### 🌐 **Локализация:**
- ✅ Полная русская локализация
- ✅ Архитектура для английского языка
- ✅ LocaleManager для переключения
- ✅ Все строки в strings.xml

## 🚧 **В разработке (Stage 10 - QA):**
- ⏳ Оптимизация производительности карты
- ⏳ Unit тесты для бизнес-логики
- ⏳ UI тесты с Compose Testing
- ⏳ Firebase Crashlytics интеграция
- ⏳ Аналитика использования
- ⏳ Уменьшение размера APK

## 📊 **Прогресс:** 91% (120/132 задач выполнено)
---

# 🏗️ Архитектура приложения

## Clean Architecture + MVVM:
- **Domain Layer** - бизнес-логика и use cases
- **Data Layer** - репозитории и источники данных
- **Presentation Layer** - UI и ViewModels

## Ключевые технологии:
- **Jetpack Compose** - современный UI toolkit
- **Hilt** - dependency injection
- **Coroutines + Flow** - асинхронность
- **Room** - локальная БД
- **Yandex MapKit** - карты
- **Coil** - загрузка изображений

## Принципы разработки:
- Single Activity Architecture
- Unidirectional Data Flow
- Repository Pattern
- Offline-First подход